#!/bin/bash

# Build up the deploy classpath
dir=$(dirname $0)
classpath=
for f in $(ls $dir/../lib); do
  classpath=$dir/../lib/$f:$classpath
done

deployXml=deploy/remote/deploy.xml

java -cp $classpath org.jcatapult.deployment.DeploymentManager $deployXml
